// Ссылка "позвонить" из введённого вручную номера — люди пишут телефон
// по-разному (с пробелами, скобками, дефисами), а tel: понимает только
// цифры и ведущий "+".
export function telHref(phone: string): string {
  return `tel:${phone.replace(/[^\d+]/g, "")}`;
}
