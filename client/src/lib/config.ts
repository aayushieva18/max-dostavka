// Настройки, которые понадобится поменять на реальные значения перед публикацией.

// Id хозяйки — по нему приложение понимает, кому показать экран курьера,
// а не экран покупателя. Пока приложение не открыто изнутри настоящего MAX
// (нужен зарегистрированный бот), настоящего id взять неоткуда — используем
// тестовое значение "owner" для проверки на этом компьютере: открой
// http://localhost:5173/?max_user_id=owner, чтобы попасть на экран хозяйки.
// Когда появится бот в MAX и настоящий id — заменить здесь.
export const OWNER_MAX_ID = "owner";

// Ключ для Яндекс Карт (JS API + геокодер + маршруты).
export const YANDEX_MAPS_API_KEY = "5ceb288a-745d-4409-b962-7008ffb371c9";

// Область, где работает доставка — весь Забайкальский край (Чита, Агинский
// округ и прочее). Поиск адреса ЖЁСТКО ограничен этой областью (не подсказка,
// а именно ограничение) — адреса за её пределами не находятся специально,
// чтобы не путать с похожими названиями улиц в других регионах России.
// При расширении зоны доставки нужно расширить и эти границы.
export const DELIVERY_REGION_BOUNDS = {
  minLat: 49.5,
  maxLat: 58.5,
  minLon: 107.0,
  maxLon: 123.5,
};

// Адрес сервера — выложен на Render 2026-09-15.
// Бесплатный тариф "засыпает" без обращений ~15 минут — первый запрос после
// паузы может занять секунд 30–50, это нормально, не ошибка.
export const SERVER_URL = "https://max-dostavka.onrender.com";
