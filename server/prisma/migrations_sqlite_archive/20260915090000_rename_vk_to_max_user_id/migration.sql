-- Переход с VK на MAX: поле, по которому опознаётся покупатель, было
-- "vkUserId", становится "maxUserId". Значения переносим как есть — они
-- всё равно были тестовыми/условными, реальные id из MAX появятся по мере
-- использования приложения.
ALTER TABLE "Customer" RENAME COLUMN "vkUserId" TO "maxUserId";
